実行したら、ターミナルに任意の数字を入力してください。
文字には対応していません。JISコード変換すれば文字対応も出来ると思うので、余力があったらやります。

出力は「ランダムな512ビットの素数」「公開鍵」「秘密鍵」「暗号文」「復号化した平文」の順にターミナル上で出力されます。

素数の桁数に関して、RSA暗号について調べたところ、一般的には1024～2048ビットや4096ビットが用いられているらしいですが、今回はあくまで動作検証という事で512ビットで生成しました。もし変更したい場合は91行目のnの値を変更してください。(大きい値にすると動作が遅くなる可能性があります)